# 内存介绍

## 内存的高位与低位

如果内存中的数据为 01 02 03 04，那么储存到寄存器就是04 03 02 01。在内存中01是低位，04是高位；在寄存器中04是高位，01是低位。

## 大小端储存模式

大端储存模式：指数据的低位保存在内存的高地址中，而数据的高位，保存在内存的低地址中；

小端储存模式：指数据的高位保存在内存的低地址中，而数据的低位，保存在内存的高地址中；

假设有`int i = 12;`

![](https://img-blog.csdnimg.cn/c4943df9ee1c4703adb5796e387b0b9d.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L0pvcm11bmdhbmRfVg==,size_16,color_FFFFFF,t_70)

### 如何判断电脑为大端储存还是小端储存

```c
int a = 66051; // 0x010203
```

如果为大端储存，则为：`内存低位 | 00 01 02 03 | 内存高位`

如果为小端储存，则为：`内存低位 | 03 02 01 00 | 内存高位`

> [!TIP] 变量指针指向变量的内存低位。如何判断呢？首先我们假设变量指针指向内存的低位，那么指针+1就应该得到02（假设指针强转为char类型，并且为小端储存的情况下），指针-1则得到未知的数。
> ```c
> int main(){
>    int a = 66051; // ?? 01 02 03 00
>    printf("next: %d last: %d",*(((char*)&a)+1),*(((char*)&a)-1));
>    return 0;
>}
> //output: next:2 last:0
> ```

所以，我们只需要执行以下代码就可以判断到底是大端模式还是小端模式：

```c
printf("%d",*(char*)a);

// output: 3 // 0x03
```

因此，可以判断我的机子是小端储存。
